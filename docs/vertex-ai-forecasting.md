# Vertex AI Forecasting

The visible bounded linear saturation model remains canonical. The supplemental API returns deterministic forecast, optional Vertex result, variance, model metadata, fallback and evidence references. Without a configured project/credentials it reports `DETERMINISTIC_BASELINE_USED`; it never presents an unavailable model as truth.
