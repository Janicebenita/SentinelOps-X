# Google ADK Runtime

`backend/app/adk` exposes the validated orchestration boundary and detects the official ADK package. In local mode it delegates through the same typed A2A contract without pretending ADK executed. In credentialed images, install/configure ADK and exercise the same boundary. ADK cannot mutate workflow state directly; the backend state machine remains authoritative.
