# Gemini Core Reasoning

Gemini is bounded to evidence correlation, contradiction/missing-evidence summaries and executive explanation. Calls store model, prompt version, artifact hash, output hash, latency, trace and fallback state. Forecasts, gates, workflow state and approvals remain deterministic/backend-controlled. Without credentials the same schema uses an explicit deterministic fallback.
