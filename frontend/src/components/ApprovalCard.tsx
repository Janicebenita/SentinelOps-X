export function ApprovalCard({ready,onApprove,onReject}:{ready:boolean;onApprove:()=>void;onReject:()=>void}){return <section className="card approval"><div><p className="eyebrow">HUMAN CONTROL POINT</p><h2>Release authorization</h2><p>One-line tax normalization · Low risk · Rollback by reverting the repair commit.</p></div><div className="actions"><button className="ghost" disabled={!ready} onClick={onReject}>Reject</button><button className="primary" disabled={!ready} onClick={onApprove}>Approve PR</button></div></section>}
