import {useQuery} from '@tanstack/react-query';
import {ShieldCheck} from 'lucide-react';
import {nexusApi} from '../api/client';
export default function ResourcePage(){const path=location.pathname;const id=Number(path.split('/')[2]);const workflows=useQuery({queryKey:['workflows'],queryFn:nexusApi.workflows});const run=workflows.data?.find(x=>x.id===id)||workflows.data?.[0];let title='SentinelOps Nexus Documentation';let body:unknown={routes:['/command-centre','/agents','/architecture','/safety','/docs']};if(path==='/architecture'){title='Architecture';body={flow:'React routes → FastAPI → deterministic workflow services → SQLAlchemy artifacts → SHA-256 audit chain',boundary:'No cloud or production execution adapter exists.'}}else if(path==='/safety'){title='Safety and Governance';body={intern:'Cannot approve',senior_developer:'May approve with a short-lived verified token and rationale',verification_agent:'Verifies but never approves',production_action:'NOT EXECUTED'}}else if(path.includes('/evidence')){title='Evidence Explorer';body=run?.id?`Open the Command Centre evidence view for workflow ${run.id}.`:''}else if(path.includes('/audit')){title='Audit Timeline';body=run?.id?`Open the Command Centre audit view for workflow ${run.id}.`:''}else if(path.includes('/verification')){title='Verification Results';body=run?.tournament_json}else if(path.includes('/export')){title='Evidence Export';body='Evidence export becomes available only after a recorded human decision and is revoked after one download.'}else if(path.startsWith('/workflows/')){title=`Workflow ${id}`;body=run}return <div className="product-page"><nav><a href="/"><b>SENTINELOPS NEXUS</b></a><a href="/command-centre">Command Centre</a></nav><main><h1>{title}</h1><article className="workspace-card"><pre>{typeof body==='string'?body:JSON.stringify(body,null,2)}</pre></article><div className="boundary"><ShieldCheck/> PRODUCTION ACTION: NOT EXECUTED</div></main></div>}
