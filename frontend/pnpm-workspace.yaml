packages:
  - .

# Pin the patched transitive CSS parser across Vite and Vitest.
overrides:
  postcss: 8.5.23

# esbuild is the only dependency permitted to run its platform-binary install.
allowBuilds:
  esbuild: true
