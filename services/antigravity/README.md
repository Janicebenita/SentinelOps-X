# Antigravity service boundary

No deployable Antigravity service is packaged because official participant
documentation, SDK/runtime, endpoint, and access were unavailable. Creating a
mock Cloud Run service would fabricate managed integration evidence.

The typed provider boundary lives in `backend/app/integrations/antigravity/`
and preserves deterministic local Digital Twin simulation. Activate a service
here only after the official contract is supplied and an authenticated smoke
test can record provider version, invocation ID, latency, workflow ID, trace ID,
and evidence ID.

Status: `DOCUMENTATION_OR_ACCESS_BLOCKED`

`PRODUCTION ACTION: NOT EXECUTED`
